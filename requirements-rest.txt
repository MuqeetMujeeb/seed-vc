accelerate
scipy==1.13.1
librosa==0.10.2
huggingface-hub>=0.28.1
munch==4.0.0
einops==0.8.0
descript-audio-codec==1.0.0
gradio==5.23.0
pydub==0.25.1
resemblyzer
jiwer==3.0.3
transformers==4.46.3
soundfile==0.12.1
sounddevice==0.5.0
modelscope==1.18.1
funasr==1.1.5
hydra-core==1.3.2
pyyaml
python-dotenv

# torch==2.4.0's compiled extensions expect the numpy 1.x ABI - installing
# without this pin lets pip resolve numpy 2.x (pulled in transitively via
# numba/librosa), which imports but logs "Failed to initialize NumPy:
# _ARRAY_API not found" and risks a crash later, not just a warning. Must be
# installed/reinstalled *after* the packages above, since several of them
# would otherwise happily pull in numpy 2.x themselves.
numpy==1.26.4
