# general things to ignore
.DS_Store
build/
build_contrib/
dist/
.cache/
*.egg-info/
*.egg
*.py[cod]
__pycache__/
*.so
*~

# IDE
.vscode/
.idea/

# misc
checkpoints/
venv/
test_waves/
reconstructed/
.python-version
ruff.log
/configs/inuse/
runs/
/garbages/
/flagged/
/experimental/

# Large Windows binaries needed on disk (pydub's mp3 export path in
# service.py) but never committed - GitHub rejected an earlier push over\n# its 100MB file size limit. Matches how Deep-Live-Cam/ handles the same\n# files (present, needed, untracked).
ffmpeg.exe
ffprobe.exe
